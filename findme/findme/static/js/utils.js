
$(function() {
    // CSS selectors passed to the jQuery object return a jQuery object with that node selected.
    $('#user-name').text('Hello First Last');
});

function imgError()
{
alert('The image could not be loaded.');
}